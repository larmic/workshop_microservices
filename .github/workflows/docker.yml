name: Docker Build & Push Go-Services

on:
  push:
    branches: [main]
    paths:
      - 'services/**'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  changes:
    runs-on: ubuntu-latest
    outputs:
      flight: ${{ steps.filter.outputs.flight }}
      hotel: ${{ steps.filter.outputs.hotel }}
      car: ${{ steps.filter.outputs.car }}
      booking-story1: ${{ steps.filter.outputs.booking-story1 }}
      booking-story2: ${{ steps.filter.outputs.booking-story2 }}
      booking-story3: ${{ steps.filter.outputs.booking-story3 }}
      booking-story4: ${{ steps.filter.outputs.booking-story4 }}
      dashboard: ${{ steps.filter.outputs.dashboard }}
    steps:
      - uses: actions/checkout@v6
      - uses: dorny/paths-filter@v4
        id: filter
        with:
          filters: |
            flight:
              - 'services/flight/**'
              - 'services/shared/**'
              - 'services/go.mod'
              - 'services/go.sum'
              - 'services/Dockerfile'
            hotel:
              - 'services/hotel/**'
              - 'services/shared/**'
              - 'services/go.mod'
              - 'services/go.sum'
              - 'services/Dockerfile'
            car:
              - 'services/car/**'
              - 'services/shared/**'
              - 'services/go.mod'
              - 'services/go.sum'
              - 'services/Dockerfile'
            booking-story1:
              - 'services/booking/story1/**'
              - 'services/shared/**'
              - 'services/go.mod'
              - 'services/go.sum'
              - 'services/Dockerfile'
            booking-story2:
              - 'services/booking/story2/**'
              - 'services/shared/**'
              - 'services/go.mod'
              - 'services/go.sum'
              - 'services/Dockerfile'
            booking-story3:
              - 'services/booking/story3/**'
              - 'services/shared/**'
              - 'services/go.mod'
              - 'services/go.sum'
              - 'services/Dockerfile'
            booking-story4:
              - 'services/booking/story4/**'
              - 'services/shared/**'
              - 'services/go.mod'
              - 'services/go.sum'
              - 'services/Dockerfile'
            dashboard:
              - 'services/dashboard/**'
              - 'services/shared/**'
              - 'services/go.mod'
              - 'services/go.sum'

  build-flight:
    needs: changes
    if: needs.changes.outputs.flight == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - uses: docker/setup-buildx-action@v4
      - uses: docker/build-push-action@v7
        with:
          context: services
          file: services/Dockerfile
          push: true
          platforms: linux/amd64,linux/arm64
          build-args: |
            SERVICE_PATH=flight
            SERVICE_DESC=Workshop Microservices - Flight Service
            SERVICE_PORT=8081
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/workshop-microservices-flight:latest

  build-hotel:
    needs: changes
    if: needs.changes.outputs.hotel == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - uses: docker/setup-buildx-action@v4
      - uses: docker/build-push-action@v7
        with:
          context: services
          file: services/Dockerfile
          push: true
          platforms: linux/amd64,linux/arm64
          build-args: |
            SERVICE_PATH=hotel
            SERVICE_DESC=Workshop Microservices - Hotel Service
            SERVICE_PORT=8082
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/workshop-microservices-hotel:latest

  build-car:
    needs: changes
    if: needs.changes.outputs.car == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - uses: docker/setup-buildx-action@v4
      - uses: docker/build-push-action@v7
        with:
          context: services
          file: services/Dockerfile
          push: true
          platforms: linux/amd64,linux/arm64
          build-args: |
            SERVICE_PATH=car
            SERVICE_DESC=Workshop Microservices - Car Service
            SERVICE_PORT=8083
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/workshop-microservices-car:latest

  build-booking-story1:
    needs: changes
    if: needs.changes.outputs.booking-story1 == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - uses: docker/setup-buildx-action@v4
      - uses: docker/build-push-action@v7
        with:
          context: services
          file: services/Dockerfile
          push: true
          platforms: linux/amd64,linux/arm64
          build-args: |
            SERVICE_PATH=booking/story1
            SERVICE_DESC=Workshop Microservices - Booking Service (Story 1)
            SERVICE_PORT=8080
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/workshop-microservices-booking-story1:latest

  build-booking-story2:
    needs: changes
    if: needs.changes.outputs.booking-story2 == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - uses: docker/setup-buildx-action@v4
      - uses: docker/build-push-action@v7
        with:
          context: services
          file: services/Dockerfile
          push: true
          platforms: linux/amd64,linux/arm64
          build-args: |
            SERVICE_PATH=booking/story2
            SERVICE_DESC=Workshop Microservices - Booking Service (Story 2)
            SERVICE_PORT=8080
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/workshop-microservices-booking-story2:latest

  build-booking-story3:
    needs: changes
    if: needs.changes.outputs.booking-story3 == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - uses: docker/setup-buildx-action@v4
      - uses: docker/build-push-action@v7
        with:
          context: services
          file: services/Dockerfile
          push: true
          platforms: linux/amd64,linux/arm64
          build-args: |
            SERVICE_PATH=booking/story3
            SERVICE_DESC=Workshop Microservices - Booking Service (Story 3)
            SERVICE_PORT=8080
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/workshop-microservices-booking-story3:latest

  build-booking-story4:
    needs: changes
    if: needs.changes.outputs.booking-story4 == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - uses: docker/setup-buildx-action@v4
      - uses: docker/build-push-action@v7
        with:
          context: services
          file: services/Dockerfile
          push: true
          platforms: linux/amd64,linux/arm64
          build-args: |
            SERVICE_PATH=booking/story4
            SERVICE_DESC=Workshop Microservices - Booking Service (Story 4)
            SERVICE_PORT=8080
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/workshop-microservices-booking-story4:latest

  build-dashboard:
    needs: changes
    if: needs.changes.outputs.dashboard == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - uses: docker/setup-buildx-action@v4
      - uses: docker/build-push-action@v7
        with:
          context: services
          file: services/dashboard/Dockerfile
          push: true
          platforms: linux/amd64,linux/arm64
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/workshop-microservices-dashboard:latest
