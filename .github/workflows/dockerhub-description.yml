name: Update Docker Hub descriptions

on:
  push:
    branches: [main]
    paths:
      - '.github/dockerhub/**'
      - '.github/workflows/dockerhub-description.yml'
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - image: workshop-microservices-flight
            short: "Flight-Service (Go-REST-API). Demo-Image fuer den Workshop 'Microservices richtig gemacht'."
            readme: .github/dockerhub/flight.md
          - image: workshop-microservices-hotel
            short: "Hotel-Service (Go-REST-API). Demo-Image fuer den Workshop 'Microservices richtig gemacht'."
            readme: .github/dockerhub/hotel.md
          - image: workshop-microservices-car
            short: "Car-Service (Go-REST-API). Demo-Image fuer den Workshop 'Microservices richtig gemacht'."
            readme: .github/dockerhub/car.md
          - image: workshop-microservices-booking
            short: "Booking-Service (Story-Auswahl per Tag: story1..story7, custom). Workshop-Demo."
            readme: .github/dockerhub/booking.md
          - image: workshop-microservices-dashboard
            short: "Dashboard-UI fuer den Workshop 'Microservices richtig gemacht'. Demo-Image."
            readme: .github/dockerhub/dashboard.md
    steps:
      - uses: actions/checkout@v7
      - uses: peter-evans/dockerhub-description@v5
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
          repository: ${{ secrets.DOCKERHUB_USERNAME }}/${{ matrix.image }}
          short-description: ${{ matrix.short }}
          readme-filepath: ${{ matrix.readme }}
